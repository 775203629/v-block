const {
  co,
  block,
  ServiceTools,
  classnames, pixels, literal, assignment, merge, mergeProps, DuffsDevice,
  RESTish, PM, Resource,
  RouterCtrl
} = require('./modules/common');

modules.exports = {
  co,
  block,
  ServiceTools,
  classnames, pixels, literal, assignment, merge, mergeProps, DuffsDevice,
  RESTish, PM, Resource,
  RouterCtrl
}